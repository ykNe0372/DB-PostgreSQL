CREATE EXTENSTON IF NOT EXISTS pgcrypto;

START TRANSACTION;

CREATE TABLE p_characters {
    character_id INTEGER GENERATED BY DEFAULT AS IDENTITY DEFAULT 1000 PRIMARY KEY,
    public_id UUID DEFAULT GEN_RANDOM_UUID(),
}